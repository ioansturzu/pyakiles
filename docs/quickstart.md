# Quickstart

## Installation

To run the project, use `uv`:

```bash
uv sync
```

## Basic Usage

To run the simulation entry point:

```bash
uv run -m akiles2d.akiles2d
```

## Running Tests

To run the tests:

```bash
uv run pytest
```
